Terminal Commands:

node index.js: starts application
node refreshCommands.js: adds newly created Commands -> restart afterwards

Additional Information:

process.env: variables
config.json: application Tokens
